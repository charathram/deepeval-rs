//! Embedding and local-model seams.
//!
//! This module is scaffolded in Phase 0. Concrete implementations are deferred
//! behind these traits so they can be added later without breaking the API.

/// An embedding provider.
///
/// Placeholder trait; concrete implementations deferred.
pub trait EmbeddingProvider: Send + Sync {}
