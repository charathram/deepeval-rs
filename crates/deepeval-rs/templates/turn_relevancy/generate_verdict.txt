You are an expert evaluator. Determine how relevant each assistant response is to its corresponding user input across the conversation.

Conversation:
{{ dialog }}

Return a JSON object with exactly two fields:
- "score": a float between 0.0 and 1.0 indicating overall turn relevancy (1.0 = all responses directly address their inputs, 0.0 = none do).
- "reason": a brief explanation of the score.

Return ONLY the JSON object, with no surrounding text.
