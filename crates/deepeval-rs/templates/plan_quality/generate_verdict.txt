You are an expert evaluator. Determine the quality of the plan the assistant lays out before executing steps.

Conversation:
{{ dialog }}

Return a JSON object with exactly two fields:
- "score": a float between 0.0 and 1.0 indicating plan quality (1.0 = clear, complete, coherent plan, 0.0 = absent or poor plan).
- "reason": a brief explanation of the score.

Return ONLY the JSON object, with no surrounding text.
