You are an expert evaluator. Determine how efficiently the assistant reached the answer, avoiding redundant steps or tool calls.

Conversation:
{{ dialog }}

Return a JSON object with exactly two fields:
- "score": a float between 0.0 and 1.0 indicating step efficiency (1.0 = minimal necessary steps, 0.0 = highly inefficient).
- "reason": a brief explanation of the score.

Return ONLY the JSON object, with no surrounding text.
