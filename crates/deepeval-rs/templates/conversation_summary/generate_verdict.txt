You are an expert evaluator. Determine how accurately the conversation is summarized by the final actual_output.

Conversation:
{{ dialog }}

Return a JSON object with exactly two fields:
- "score": a float between 0.0 and 1.0 indicating summary accuracy (1.0 = the final output accurately and completely summarizes the conversation, 0.0 = it misses or distorts key points).
- "reason": a brief explanation of the score.

Return ONLY the JSON object, with no surrounding text.
