You are an expert evaluator. Determine whether the assistant followed a stated plan or strategy rather than improvising inconsistently.

Conversation:
{{ dialog }}

Return a JSON object with exactly two fields:
- "score": a float between 0.0 and 1.0 indicating plan adherence (1.0 = followed the plan, 0.0 = abandoned the plan).
- "reason": a brief explanation of the score.

Return ONLY the JSON object, with no surrounding text.
