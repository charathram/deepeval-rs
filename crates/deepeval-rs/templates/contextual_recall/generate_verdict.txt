You are an expert evaluator. Determine how well the retrieval context supports the ground-truth facts in the expected output.

Input:
{{ input }}

Actual Output:
{{ actual_output }}

Expected Output (ground truth):
{{ expected_output }}

Retrieval Context:
{{ retrieval_context }}

Score how many of the ground-truth facts in the expected output are supported by the retrieval context.

Return a JSON object with exactly two fields:
- "score": a float between 0.0 and 1.0 indicating contextual recall (1.0 = all ground-truth facts are supported, 0.0 = none are supported).
- "reason": a brief explanation of the score.

Return ONLY the JSON object, with no surrounding text.
